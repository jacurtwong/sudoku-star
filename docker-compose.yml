name: sudoku-star

services:
  sudoku:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sudoku-star
    ports:
      - "8180:3000"
    volumes:
      - sudoku-star-data:/app/data
    restart: unless-stopped

volumes:
  sudoku-star-data:
